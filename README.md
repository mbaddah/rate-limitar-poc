# Rate limiter poc

### To-do

- Use Redis to store requests counts per ID + user ID
- Develop various algorithm examples using:
  - token bucket
  - leaking bucket
  - fixed window
  - sliding window